# Copy to `.env` in the project root (next to package.json). Never commit real keys.

# DeepSeek (https://platform.deepseek.com/) — used by `npm run api` (server/chat-server.mjs)
DEEPSEEK_API_KEY=sk-ff11ab8564a64541a184e0ae1a1defbd
# Optional: deepseek-chat (default), deepseek-reasoner, etc.
DEEPSEEK_MODEL=deepseek-chat

PORT=8787

# Optional: .env.local overrides .env (gitignore it)
# VITE_CHAT_API_URL=
